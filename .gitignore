# Referencia del esquema original (no es parte de GrowFlow)
database-ref/

# Supabase local
supabase/.temp/
supabase/.branches/

# Artefactos de sesiones
.playwright-mcp/

# telemetria de claude-flow, no es codigo de la app
.claude-flow/
scripts/manual.html

# Variables de produccion: NUNCA versionadas (contienen credenciales de acceso)
.env.production
app/.env.production
